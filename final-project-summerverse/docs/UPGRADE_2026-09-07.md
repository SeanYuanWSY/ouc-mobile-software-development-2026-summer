# 2026-09-07 本地改进记录

## 新版部署接续

### 2026-09-12 资料云端部署

【已核实】资料库ADMINONLY、精确存储authority、dataService/deepseekProxy部署和云包下载哈希核对完成；1.0.6已确认为体验版，备案/认证已完成，无审核/线上版本。此结果取代下方9月11日尚未部署的状态。隐私指引已补齐用户指定邮箱并提交，后台显示审核中；待审正文回读异常，保存完整性尚未确认。真实微信身份及BYOK验收未完成。证据统一在 [ACCEPTANCE_STATUS.md](ACCEPTANCE_STATUS.md) 最新节。

### 2026-09-11 资料分析第一版

【已核实】本地新增从聊天文件/图片/文字导入到有出处分析、确认任务和资料库恢复的流程。支持PDF文字、DOCX、PPTX、TXT/MD和图片文字转写；没有完整群聊读取或原生语音权限。本次118项本地测试、9类原生页面流程验证及安全定点复核完成，证据统一在 [ACCEPTANCE_STATUS.md](ACCEPTANCE_STATUS.md) 与 [MATERIALS.md](MATERIALS.md)，不以本地演示代替真实AI效果验收。

【已核实】已准备隔离部署副本，本次没有更新腾讯云和手机体验版、没有推送GitHub。部署接续需新增material_workspaces及ADMINONLY权限、两云函数的非敏感存储authority配置、更新解析依赖和前端。旧SDK依赖审计告警仍保留为部署前待核查项，具体见上述验收状态。

### 2026-09-10 ego 云端准备

【已核实，后续】两新函数现已回读为 Active / Nodejs20.19 / 20秒。数字输入直接填值未可靠保存，使用控件加号后成功。新建 /assistant→assistantGateway 路由保持关闭；网关限频为标准版能力，个人版触发升级提示，已取消未购买。日志服务未开启。公网联调及体验版仍待完成，详见 ASSISTANT_BRIDGE.md。

【已核实】ego 恢复腾讯云页面控制，指定环境三个 assistant_* 新集合创建并逐项回读为 ADMINONLY。仅部署 assistantInbox、assistantGateway，CLI 两项 success:true；无 HTTP 路由、未替换体验版。此状态取代9月9日“尚未创建/部署”的历史描述。

【已核实】CLI 新建默认 Nodejs16.13，控制台不支持直接修改运行环境。用户确认后已仅重建这两个新函数，CLI回读均 Active / Nodejs20.19，取代此前待重建状态。完整云端联调、超时回读、日志/限流和新版体验版仍未完成。细节见 ASSISTANT_BRIDGE.md。

### 2026-09-09 外部助手第一版

【已核实】本地新增 assistantInbox（微信身份管理/接受草稿）、assistantGateway（Bearer业务接口）、本机stdio MCP适配器、灵感收件箱页面与设置入口。默认仅投递、显式近期读取、30天连接、撤销、事务限额和接受幂等。源码说明见 ASSISTANT_BRIDGE.md。

【已核实】安全前置与定点复核完成，修正空正文回环和页面隐藏后的迟到凭证；87项本地测试通过。微信开发工具曾检出WXML转义表达式编译错误，已改为原生表达式。随后模拟器可编译首页，但自动化响应超时及原生工具noWindowsAvailable阻止完成收件箱视觉验收，不宣称全部页面验收通过。

【已核实】本轮腾讯云内置浏览器控制连续超时；未创建新集合、未部署新函数、未开放HTTP、未上传替换体验版或推送GitHub。现有1.0.5保持不变。待恢复控制台后按 ASSISTANT_BRIDGE.md 完成集合权限、日志与限流核查，再真实联调。

### 2026-09-09 云端权限核查

【已核实】微信公众平台登录后，首页显示“晞屿手记”、微信认证已完成、小程序备案“管局审核中”。版本管理显示 1.0.5 标记为体验版（提交时间 2026-09-07 15:02:16），线上版本“尚未提交线上版本”，无正在审核的代码版本。本条取代此前“未核实体验版切换”的状态；备案通过、代码审核和正式发布仍未完成。本轮未提交审核或发布。

【已核实】本轮再次运行 npm run verify，项目检查和 75 项测试全部通过；不替代真实 AI 接口及云端分页验收。

【已核实】登录恢复后，通过腾讯云控制台逐个查看 ai_usage、goals、media_assets、memories、profiles、step_snapshots 的权限设置，均为 ADMINONLY。云存储为“仅创建者及管理员可读写”，临时下载链接有效期 10 分钟。本轮未更改权限、计费或用户数据；这些配置检查不替代云函数跨用户隔离的实际验收。

【已核实】memories 索引管理显示 openid_occurredAt（_openid 升序、occurredAt 降序）、_id_ 和 _openid_1；未显示包含 _id 排序字段的三字段复合索引。当前代码还按 _id 降序排序，实际云端分页能否直接执行仍待验证，不据此断言已发生查询错误。本轮未创建索引。控制台曾短暂显示 DescribeEnvBacklogs 网络错误，随后索引字段正常加载；该提示不作为业务查询失败证据。

### 2026-09-08 云端配置更新

【已核实】Computer Use 已恢复腾讯云页面控制。经定点前置审查，仅将 deepseekProxy 执行超时设为 90 秒、dataService 设为 20 秒；保存后页面显示对应数值，官方开发工具 CLI 回读均 Active。此前“3 秒仍阻塞、未调整”的描述为历史状态，已由此次配置取代。内存仍 256 MB，未改 Key、网络权限或触发器。

【已核实】当前环境套餐页显示个人版，有效期至 2026-10-08 23:59:59，自动续费未开通、按量付费未勾选，资源额度正常。数据回档列在升级能力中；未购买、续费或切换计费模式。证据为本轮腾讯云套餐用量页面。

【已核实】以上仅完成运行配置；真实用户 AI 文字/图片响应、云端分页与体验版切换仍未验收。客户端文字/图片等待仍为 60/65 秒，服务器 90 秒不是端到端成功保证。

【已核实】2026-09-07 用户扫码后，官方开发者工具 CLI 已将 dataService、deepseekProxy 部署到 cloudbase-d5gdro8i30f1a4efd，两者均返回 success:true；随后 functions info 回读均 Active / Nodejs20.19 / timeout 3 秒。原“尚未部署”状态由本次成功部署取代。浏览器控制 getTab 持续超时，原生开发工具菜单未提供远端超时配置入口，超时调整仍未完成。

【已核实】已补旧客户端兼容：memory.list 无 offset 时保留默认 200 / 显式最多 300 条，内部分批读取；新客户端带 offset 保留分页。新增云函数执行测试，全套 75/75 通过。

【已核实】干净上传副本：/var/folders/mj/b74s97nn2fx6cxlhmrmzld5r0000gn/T/summerverse-release-EhDCAY；126 个文件逐项 SHA-256 验证，排除私有配置，release-manifest.json 在副本根。前端环境 ID 明确绑定上述环境。官方 CLI 已成功上传开发版本 1.0.5，包大小 1,414,058 字节；upload-result.json 位于该副本根。尚未核实公众平台已将它设为体验版。

【已核实】本轮已部署两项云函数并上传前端开发版本；未正式发布、未推送 GitHub、未调用真实付费 AI。原体验版不会因为上传开发版本就被认为已切换。3 秒云端超时仍是 AI 验收阻塞项。

## 已完成

- 增加 DeepSeek、GLM、Kimi、通义千问和自定义 OpenAI Chat Completions 接口；文字与识图分别选择。
- 服务商、模型和无凭据的地址保留在本机。Key 仍仅在本次运行中使用，长期保存方式待用户决定。
- 自定义地址限制公网 HTTPS；检查全部 DNS 结果并固定实际连接地址，拒绝内网、重定向、URL 凭据和查询参数。
- 切换接收方清除旧 Key 和授权；设置变更会阻止待发请求，旧回复不能改变新配置状态。
- 发送 Key 前先探测云函数协议，旧代理只收到无 Key 的能力探测。
- 记忆列表按 100 条分页，搜索空页仍继续读取；移除本机默认 300 条截断。
- 精简首页、记录、成长、分身、报告和设置的重复文字。权限、备份与 AI 说明折叠，保留 AI 标签和授权提示。

## 保存边界

【已核实】云模式下，记忆、目标和资料由 dataService 按 OPENID 保存，代码没有到期删除。正常关闭不会主动清空。云资源可用性和主动删除仍会影响数据。本机与云数据分开，尚无自动迁移。

【已核实】聊天历史仍是本机最近 40 条，尚无账号云同步。API Key 尚未做本机记住或云端加密保存。不要宣称所有内容已跨设备永久保存。

## 动态外观

【已核实】小岛为三张阶段插画：0 条初生、1–59 条成长、60 条及以上成熟。分身花园按“记录数 + 2 × 本机历史用户消息数”计算，8 和 60 为换图节点。头像固定；不是持续生成画面或实时 3D。删除记录或清理聊天可能降低阶段。

## 发布顺序与未验证项

1. 先更新 dataService 和 deepseekProxy，再上传前端。memory.list 新增 nextOffset；新前端遇到旧服务端会要求更新。
2. 记忆查询按 _openid 过滤，occurredAt desc、_id desc 排序；如云端要求索引，按查询创建复合索引并实测分页。
3. 核对代理总超时覆盖 DNS、鉴权、照片下载和上游等待；前端文字等待 60 秒、照片 65 秒，按实际耗时协调。旧的短超时不能用于验收。
4. 更新公众平台第三方服务说明，核对平台调用追踪和日志不保存用户 Key。
5. 真实 Key 的文字、图片调用和云端分页尚未验收；本地通过不代表付费接口已接通。

## 本地验证

【已核实】微信开发者工具隔离副本检查五种服务商选择、切换清 Key、自定义输入、折叠项，以及记录、成长、分身和报告页面。未发送真实 API 请求。

【已核实】安全定点复核修正 IPv6 特殊地址判断、误填 URL 凭据持久化、异步撤销竞态；新旧代理协议检查通过。测试位于 tests/providers.test.js、tests/byok.test.js 和 tests/repository.test.js，最终使用 npm run verify。

## 官方模型资料

- [DeepSeek 识图](https://api-docs.deepseek.com/guides/vision/)
- [GLM 文字](https://docs.bigmodel.cn/cn/guide/models/text/glm-5.2)、[GLM 识图](https://docs.bigmodel.cn/cn/guide/models/free/glm-4.6v-flash)
- [Kimi K2.6](https://platform.kimi.com/docs/guide/kimi-k2-6-quickstart)
- [通义千问视觉接口](https://help.aliyun.com/en/model-studio/qwen-vl-compatible-with-openai)
