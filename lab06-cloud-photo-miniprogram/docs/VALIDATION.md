# 实验六验证说明

验证日期：2026-09-08。微信开发者工具 Stable 2.02.2608040，基础库 3.17.2；环境 `cloudbase-d5gdro8i30f1a4efd`。

## 实际云配置

- 集合 `lab06_photos`：所有用户可读、创建者可写；已有三条实际作品记录。
- 云函数 `lab06_getOpenid` 与 `lab06_photoAccess` 已部署并实际调用。
- `lab06_feed_created_id`：createdAt 降序、_id 降序。
- `lab06_owner_created_id`：_openid 升序、createdAt 降序、_id 降序。
- 两个复合索引均经云端读回确认，完整定义见 `../database.indexes.json`。
- 共享存储桶保持创建者私有读写，只通过实验图片访问函数签发匹配记录的临时地址。

## 实测结果

| 场景 | 结果 |
| --- | --- |
| 上传 | 三张原创图经过真实云存储上传和数据库写入，记录作者与文件路径一致 |
| 首页、作者、详情、历史 | 同一组三条记录正常展示；重开工程后仍可读取 |
| 原生选图 | 实际打开系统选择器，选图后正确回填文件路径和大小 |
| 下载 | 真实签名地址下载 HTTP 200、downloadFile:ok |
| 保存 | 点击页面下载按钮，经过系统保存窗口后显示“已保存到相册”；保存文件与原图 SHA-256 一致，40991 字节 |
| 预览 | 原生全屏预览正常显示图片 |
| 分享 | 原生卡片预览标题正确；详情路径含记录 ID；预览后取消，没有发给真实好友 |
| 回归 | 25 项 Node 测试通过，包含回调顺序、下载/保存失败和权限错误分支 |

截图见 [screenshots](screenshots/)。测试图上的 `LAB 06 / CLOUD UPLOAD TEST` 说明其用途；它们是实际上传的记录，并非界面 mock 数据。原生选图另作复验，没有再次发布重复作品。

## 实测边界

- 模拟器保存使用 macOS 文件对话框，不能等同于手机相册权限行为。
- 未验证第二个微信账号的公开读取、越权写入拒绝和好友分享送达。
- 真实数据为三条；超过 20 条数据的分页仅有逻辑测试，未进行云端大数据量实测。
- 页面截图、云端验证不代表小程序正式发布或体验版上传。

## 回归方法

在实验目录运行 `npm test`。`photos.test.js` 检查身份、发布恢复、排序、签名访问范围；`native.test.js` 检查 DownloadTask 立即返回时仍等待成功回调，下载失败或非 200 不进入保存，权限拒绝及设置/弹窗失败不误报成功且释放按钮状态。

## AI 扩展验证（2026-09-08）

- 42 项本地测试通过；独立定点审查覆盖凭据流、过期截止时间、配额事务、图像字节、流上限、重定向和模型限制。
- 模拟器设置页显示可选模型与密码输入框；切换 Pro 后 vision=false，再切回视觉模型，结果正确。截图：`screenshots/08-ai-settings.jpg`。
- lab06_aiReview 已部署 Active，timeout=60，Nodejs16.13；真实云调用无 Key 返回 `{ok:false,code:"KEY"}`，假 Key 配文本模型返回 `{ok:false,code:"MODEL"}`，均不发往 DeepSeek。
- lab06_ai_usage 权限界面 ADMINONLY 单选值为1；小程序查询被权限拒绝。云环境日志服务未启用。
- 未输入真实 Key，未验证收费接口的成功识图或真机 AI 交互。原相册功能验证保持上述记录。
