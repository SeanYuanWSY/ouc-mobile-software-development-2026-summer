<view class="page">
<view wx:if="{{loading}}" class="empty">正在打开这一刻…</view>
<view wx:if="{{error}}" class="notice">
<text>{{error}}</text>
<button class="small" bindtap="load">重新加载</button>
</view>
<block wx:if="{{photo}}">
<view class="eyebrow">A MOMENT TO KEEP</view>
<view class="hero-title">{{photo.title}}</view>
<navigator class="row author" url="/pages/homepage/homepage?id={{photo._openid}}">
<view class="avatar">{{photo.initial}}</view>
<view>
<view>{{photo.nickName}}</view>
<view class="meta">{{photo.dateLabel}} · {{photo.location || '日常记录'}}</view>
</view>
<text class="arrow">↗</text>
</navigator>
<view wx:if="{{photo.imageError}}" class="notice">暂时无法读取图片，请刷新后重试。<button class="small" bindtap="load">重新获取图片</button></view>
<image wx:if="{{photo.displayUrl}}" class="detail-photo" src="{{photo.displayUrl}}" mode="widthFix" bindtap="preview"/>
<button class="primary" bindtap="download" loading="{{saving}}" disabled="{{saving || !photo.displayUrl}}">下载到本地相册</button>
<view class="row actions">
<button open-type="share">分享给好友 ↗</button>
<button bindtap="preview">全屏预览 ⤢</button>
</view>
<view class="ai-panel card">
<view class="eyebrow">AI CREATIVE COMPANION</view>
<view class="section-title">让照片说更多</view>
<view class="subtitle">仅为你自己的作品生成灵感草稿。</view>
<view class="style-options"><button wx:for="{{aiStyles}}" wx:key="id" class="small {{aiStyle === item.id ? 'selected' : ''}}" data-style="{{item.id}}" bindtap="chooseAiStyle" disabled="{{aiBusy}}">{{item.label}}</button></view>
<view wx:if="{{aiError}}" class="notice">{{aiError}}</view>
<view wx:if="{{aiText}}" class="ai-result"><view class="ai-label">✦ AI 生成 · 仅供参考</view><text selectable>{{aiText}}</text></view>
<button wx:if="{{aiReady}}" class="primary" loading="{{aiBusy}}" disabled="{{aiBusy}}" bindtap="review">{{aiBusy ? '正在认真看照片…' : '生成 AI 点评'}}</button>
<button wx:else class="primary" bindtap="aiSettings">填写 API Key，启用点评</button>
<button class="small" bindtap="aiSettings">AI 设置</button>
</view>
</block>
<button class="small" bindtap="home">返回社区首页</button>
</view>
