# 本人作品删除

只接受photoId或action=pending。平台OPENID验证本人及精确文件路径后，在事务中标记deleted并固定cleanupFileID，再删除该单一文件。返回结果逐项核对；成功写fileCleaned，失败留待本人从上传页重试。重复完成请求不再次删除。普通列表、详情、签名和AI忽略deleted记录。

保留最小清理记录及历史昵称，标题/地点在移除时清空，云图片删除后不可恢复；此函数不执行批量删除，不接收客户端指定的文件地址。不应将清理记录称为彻底清空全部数据库元数据。
